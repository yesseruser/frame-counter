[project]
name = "frame-counter"
version = "0.1.0"
description = "Frame counter CLI for speedrun verification"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
frame-count = "frame_counter:main"
